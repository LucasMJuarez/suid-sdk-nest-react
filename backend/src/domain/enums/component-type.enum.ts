/**
 * Domain Enum: ComponentType
 * 
 * Define los tipos de componentes disponibles en el dominio.
 */

export enum ComponentType {
  BUTTON = 'button',
  INPUT = 'input',
  ALERT = 'alert',
  CONTAINER = 'container',
}
